import userResolver from "./user.js";
import projectResolver from "./project.js";
import countryResolver from "./country.js";

const resolvers = [userResolver, projectResolver, countryResolver];

export default resolvers;
